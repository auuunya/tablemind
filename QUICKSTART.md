# 快速启动指南

## 启动开发服务器

### 方法1：使用启动脚本（推荐）

```bash
./start.sh
```

这个脚本会自动检查并清理端口5000，然后启动开发服务器。

### 方法2：手动启动

```bash
# 如果端口5000被占用
lsof -ti:5000 | xargs kill -9

# 启动服务器
npm run dev
```

## 访问应用

开发服务器启动后，在浏览器中访问：

```
http://localhost:5000
```

## 第一次使用

1. 打开浏览器访问应用
2. 会自动弹出新手引导对话框
3. 点击"开始引导"跟随步骤学习使用
4. 或者点击右上角的"帮助中心"查看完整文档

## 主要功能

- **交互式引导**：手把手教你使用
- **帮助中心**：随时查找问题解决方案
- **错误友好**：遇到问题会有清晰的提示和建议
- **统计仪表板**：一眼看懂对比结果
- **数据安全**：所有处理在本地，不上传数据

## 查看完整文档

详细的使用说明请查看：[README.md](./README.md)

## 问题排查

### 端口被占用

如果看到错误：`Error: listen EADDRINUSE: address already in use 0.0.0.0:5000`

解决方法：
```bash
# 查找占用端口的进程
lsof -i :5000

# 停止该进程
kill -9 <PID>

# 或使用启动脚本自动处理
./start.sh
```

### 依赖问题

如果遇到模块找不到的错误：
```bash
npm install
```

## 技术支持

- 📧 Email: support@example.com
- 📖 查看: [完整文档](./README.md)
